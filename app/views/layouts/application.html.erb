<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ContactBook</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Noto+Sans+JP:400,700,900&display=swap">
  </head>

  <body>
    <header class="header">
      <div class="header__bar row">
        <h1 class="title"><a href="/">ContactBook</a></h1>
        <div class="user_nav">
          <div class='lists-right'>
            <% if user_signed_in? %>
              <li><%= link_to current_user.child_first_name, user_path(current_user.id), class: "user-childname" %></li>
              <li><%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class: "logout" %></li>
            <% else %>
              <li><%= link_to 'ログイン', new_user_session_path, class: "login" %></li>
              <li><%= link_to '新規登録', new_user_registration_path, class: "sign-up" %></li>
            <% end %>
          </div>
        </div>
      </div>
    </header>
    <%= yield %>
    <footer>
      <p>
        Copyright ContactBook 2023.
      </p>
    </footer>
  </body>
</html>
